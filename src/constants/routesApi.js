const urlDefault = {
  url: 'http://localhost',
  port: ':55816',
};

const { url, port } = urlDefault;

const urlLogin = `${url}${port}/login`;

const urlRegister = `${url}${port}/user`;

const urlPublications = `${url}${port}/publications`;

const urlComments = `${url}${port}/comments`;

const urlTechnologies = `${url}${port}/technologies`;

//userId
//description
//publicationId